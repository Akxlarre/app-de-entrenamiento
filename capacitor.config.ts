import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.entrenamiento',
  appName: 'app-de-entrenamiento',
  webDir: 'dist/app-de-entrenamiento/browser'
};

export default config;
