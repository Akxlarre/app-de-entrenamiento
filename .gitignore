# Node
node_modules/
npm-debug.log
yarn-error.log

# Angular
.angular/
dist/
tmp/
out-tsc/
bazel-out/

# Entornos y credenciales
.env
.env.*
!.env.example
# SEC-T03: environment.prod.ts si contiene claves reales hardcodeadas.
# Usa variables de entorno en CI/CD en su lugar (ver src/environments/environment.prod.ts).
# environment.prod.ts  ← descomentar si hardcodeas claves aquí

# IDEs y Editores
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Agentes de IA e Indexed (Koa Blueprint)
.mcp.json
.claude/logs/
.claude/temp/
.gemini/

# Índices: los escritos a mano SÍ se versionan (son memoria institucional y una
# sesión remota clona sin ellos). Los 5 que genera `npm run indices:sync` desde
# el AST no, para no ensuciar el repo con artefactos ni generar conflictos.
indices/COMPONENTS.md
indices/SERVICES.md
indices/FACADES.md
indices/MODELS.md
indices/USAGE-MAP.md

# Contexto privado del agente (dominio, sesión, decisiones)
context/

# Error-Driven Learning — mapa de calor local (compartir es opt-in vía npm run koa:share-errors)
error-value.json

# Playwright E2E
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
